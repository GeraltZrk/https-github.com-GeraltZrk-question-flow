# fixture: use the frozen demo data; live: call the real multimodal pipeline.
QUESTIONFLOW_MODE=fixture

# Server-side only. Never expose this with a NEXT_PUBLIC_ prefix.
OPENAI_API_KEY=
QUESTIONFLOW_COMPILER_MODEL=
QUESTIONFLOW_CRITIC_MODEL=
